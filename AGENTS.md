
## Tool Usage
- **Semantic Search**: Use `mgrep` for high-level concept searches.
- **File Operations**: Always verify file content with `read_file` before editing.
- **Codebase Analysis**: Use `codebase_investigator` for complex architectural questions.

